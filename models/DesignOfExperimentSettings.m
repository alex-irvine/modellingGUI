classdef DesignOfExperimentSettings
    
    properties
        UpperBound = []; %vector of length d representing the upper range of each dim
        LowerBound = []; %vector of length d representing the lower range of each dim
    end
    
    methods
    end
    
end

